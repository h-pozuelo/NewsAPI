import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'Plantilla con Tabs',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
